---
default_context:
  initial_user_name: "admin"
  # change this in real use
  initial_user_password: "admin-plone-123"

  wsgi_listen: localhost:8080

  # zcml diretives/meta for additional packages:
  zcml_package_metas: collective.monkeypatcher

  db_storage: zeo
  db_blob_mode: shared
  db_blob_location: "{{cookiecutter.location_clienthome}}/zeo/var/blobstorage"
  db_zeo_server: 127.0.0.1:8100
  db_zeo_name: 1
